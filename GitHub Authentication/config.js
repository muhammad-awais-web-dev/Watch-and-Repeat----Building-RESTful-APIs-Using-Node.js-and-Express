const config = {
    PORT: process.env.PORT || 3000,
    GITHUB_CLIENT_ID: process.env.GITHUB_CLIENT_ID || 'your_client_id_here',
    GITHUB_CLIENT_SECRET: process.env.GITHUB_CLIENT_SECRET || 'your_client_secret_here',
}

module.exports = config;